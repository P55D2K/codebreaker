#include <bits/stdc++.h>
#include <hashcollision.h>
using namespace std;

void end2020(int M, int Q) {
    for (int i = 0; i < Q; i++) {
        
    }
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    return 0;
}