#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int passage_number;
    cin >> passage_number;

    if (passage_number == 1)
        cout << "";
}