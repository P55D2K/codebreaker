#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    long long n; cin >> n;
    cout << n << endl;

    // while get_ultrasound() != 0:
    //     print(get_ultrasound())
    // go(20, 20, 0.3)

    return 0;
}