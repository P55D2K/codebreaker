#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    ll n_amt; cin >> n_amt;
    vector<ll> n(n_amt); stack<ll> useful;
    for (ll i = 0; i < n_amt; i++) { cin >> n[i]; }

    return 0;
}